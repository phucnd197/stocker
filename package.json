{
  "name": "stocker",
  "private": true,
  "workspaces": [
    "apps/frontend",
    "apps/backend/src",
    "apps/backend/tests",
    "packages/*"
  ],
  "packageManager": "npm@11.6.2",
  "scripts": {
    "dev": "turbo run dev",
    "test": "turbo run test",
    "build": "turbo run build",
    "codegen": "turbo run codegen",
    "prepare": "husky"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
